<div class="panel panel-default">
    <div class="panel-body">
        <p class="text-left h3 text-muted">
          <b>國立台北超強大學　成績單</b>
      </p>
    </div>
</div>

<p id="notice"><%= notice %></p>

<h1><b>成績列表</b></h1>

<%= link_to '新增學生', new_student_path,class:"btn btn-success" %>

<br /><br /><br />
<p>
  分數級別說明：90分以上 S / 80 ~ 90 : A / 70 ~ 79 : B級 / 60 ~ 69 : C級 / 未滿60 : D級
</p>

<table class="table">
  <thead>
    <tr>
      <th>姓名</th>
      <th>學號</th>
      <th>成績</th>
      <th colspan="3">處理</th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.name %></td>
        <td><%= student.student_id %></td>
        <td><%= grades_method(student) %></td>
        <td><%= link_to '編輯', edit_student_path(student),class:"btn btn-success"%></td>
        <td><%= link_to '刪除', student, method: :delete, data: { confirm: 'Are you sure?' },class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


